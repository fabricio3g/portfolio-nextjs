/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.imgur.com', 'www.upwork.com', 'socialify.git.ci'],
  },
}

module.exports = nextConfig
